import {project} from "../../project.js"

project.locales.en = {
}

project.locales.ru = {
}

project.key = {
}

project.getAssets = () => {
    return {
        texture: {
            particle: "particle.png"
        },
        sound: {
        }
    }
}

project.init = (texture) => {
    project.scene = [
    ]

    project.actions = [
    ]

    project.update = () => {

    }
}