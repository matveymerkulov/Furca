import {project} from "../../project.js"

project.locales.en = {
}

project.locales.ru = {
}

project.key = {
}

project.getAssets = () => {
    return {
        texture: {
        },
        sound: {
        }
    }
}

project.init = (texture) => {
    project.registry = {
    }

    project.scene = [
    ]

    project.actions = [
    ]

    project.update = () => {

    }
}